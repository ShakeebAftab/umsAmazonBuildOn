#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createStudentsInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createStudentsInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Students $colStr VALUES $valStr", "SELECT * FROM Students ORDER BY rollNum DESC LIMIT 1"]
}